{
  "name": "marquedefabrique",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:demo": "npm run build --workspace=packages/demo",
    "dev:demo": "npm run dev --workspace=packages/demo",
    "build:lib": "npm run build --workspace=packages/lib",
    "publish:lib": "npm run build --workspace=packages/lib && npm publish --workspace=packages/lib"
  }
}
